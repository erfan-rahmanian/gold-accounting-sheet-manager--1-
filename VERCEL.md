# راهنمای استقرار روی Vercel

این راهنما مراحل به‌روزرسانی برنامه را روی Vercel توضیح می‌دهد.

**نسخه جدید شامل:**
- سیستم چندکاربره با نام کاربری و رمز عبور
- ذخیره‌سازی ابری داده‌ها در Vercel Blob
- جداسازی کامل داده‌های هر کاربر

---

## خلاصه‌ی کار

پروژه از قبل روی GitHub و Vercel بالاست، پس ترتیب کار این است:

| # | کار | کجا |
|---|-----|-----|
| ۰ | گرفتن بک‌آپ کامل از سایت فعلی | داخل برنامه |
| ۱ | ساخت Blob Store و وصل کردنش به پروژه | داشبورد Vercel |
| ۲ | تنظیم `AUTH_SECRET` و `SIGNUP_CODE` | داشبورد Vercel |
| ۳ | `git push` | ترمینال |
| ۴ | ساخت حساب اول | سایت |
| ۵ | ساخت حساب کاربر دوم با کد دعوت | سایت |

⚠️ **مرحله‌ی ۰ اختیاری نیست.**

نسخه‌ای که الان روی Vercel بالاست هیچ بک‌اندی ندارد (فقط فایل‌های ساکن است)،
بنابراین همه‌ی داده‌هایی که تا امروز در سایت وارد کرده‌اید **فقط در حافظه‌ی
مرورگر خودتان (localStorage) ذخیره شده‌اند** — نه روی سرور.

نسخه‌ی جدید داده‌ها را از سرور می‌خواند و کلید ذخیره‌سازی مرورگر هم عوض می‌شود،
پس آن داده‌ها **خودبه‌خود منتقل نمی‌شوند**.

👈 **قبل از push، با همان مرورگر و همان دستگاهی که همیشه با آن کار می‌کنید**
وارد سایت شوید، به تب «پشتیبان‌گیری و بازیابی» بروید و یک فایل JSON کامل
دانلود کنید. بعد از ساخت حساب، همان فایل را بازیابی می‌کنید.

---

## مرحله ۱: ایجاد Blob Store

**قبل از push کردن کد،** ابتدا یک فضای ذخیره‌سازی Blob بسازید و به همین پروژه‌ای
که الان روی Vercel بالاست وصل کنید:

1. وارد داشبورد Vercel شوید
2. به تب **Storage** بروید
3. روی **Create Database** کلیک کنید
4. **Blob** را انتخاب کنید
5. **Continue** بزنید و دسترسی را روی **Private** بگذارید
6. نامی برای آن انتخاب کنید (مثلاً `gold-accounting-storage`)
7. **Create** کنید
8. در پنجره‌ی **Connect Project**، پروژه‌ی فعلی برنامه را انتخاب کنید و **Connect** بزنید

> ⚠️ **حتماً Private را انتخاب کنید، نه Public.**
> در حالت Public هر کسی که آدرس فایل را داشته باشد می‌تواند بدون رمز
> محتوایش را بخواند. کد برنامه برای حالت Private نوشته شده و همه‌ی
> خواندن/نوشتن‌ها با احراز هویت انجام می‌شود.
> **این تنظیم بعد از ساخت Store قابل تغییر نیست** — اگر اشتباه ساختید،
> Store را حذف کنید و از نو بسازید.

با اتصال به پروژه، Vercel خودش متغیر `BLOB_READ_WRITE_TOKEN` را به پروژه اضافه می‌کند
(لازم نیست دستی واردش کنید).

---

## مرحله ۲: تنظیم متغیرهای محیطی

### ۲.۱ ساخت `AUTH_SECRET`

این کلید برای امضای نشست‌های کاربری (session) و ساختن نام فایل‌های داده استفاده می‌شود. یک کلید تصادفی بسازید:

```bash
openssl rand -base64 32
```

یا از هر ابزار تولیدکننده رشته تصادفی دیگری استفاده کنید.

> ⚠️ **این کلید را یک بار تنظیم کنید و هرگز عوض نکنید.**
> نام فایل‌های داده در Blob از روی همین کلید ساخته می‌شود. اگر بعداً آن را تغییر
> دهید، برنامه دیگر فایل‌های قبلی را پیدا نمی‌کند و همه‌ی حساب‌ها و داده‌ها
> «ناپدید» می‌شوند (فایل‌ها پاک نمی‌شوند، ولی دیگر خوانده نمی‌شوند).
> پیش از هر تغییری، حتماً از داخل برنامه بک‌آپ کامل بگیرید.

### ۲.۲ انتخاب `SIGNUP_CODE`

این کد دعوت است که کاربران دوم به بعد برای ثبت‌نام نیاز دارند (کاربر اول بدون کد ثبت‌نام می‌کند).  
مثال: `my-secret-invite-2024`

### ۲.۳ افزودن به Vercel

1. در داشبورد Vercel، پروژه را باز کنید
2. به **Settings** → **Environment Variables** بروید
3. دو متغیر زیر را اضافه کنید:

   | Key                     | Value                                 |
   |-------------------------|---------------------------------------|
   | `AUTH_SECRET`           | کلید تصادفی که در مرحله ۲.۱ ساختید     |
   | `SIGNUP_CODE`           | کد دعوت دلخواه شما                     |

4. برای هر دو، محیط **Production**, **Preview** و **Development** را فعال کنید
5. **Save** کنید

---

## مرحله ۳: push کردن تغییرات

پروژه از قبل به مخزن GitHub وصل است و Vercel هر push روی شاخه‌ی اصلی را
خودکار Deploy می‌کند. پس فقط کافی است تغییرات را بفرستید:

```bash
git add -A
git commit -m "feat: چندکاربره + ذخیره‌سازی ابری"
git push
```

> ⚠️ **پیش از push، حتماً مرحله‌های ۱ و ۲ را انجام داده باشید.**
> اگر Blob و `AUTH_SECRET` تنظیم نشده باشند، Deploy موفق می‌شود ولی صفحه‌ی
> ورود با خطای سرور بالا می‌آید.

Vercel به‌محض دریافت push شروع به build می‌کند. در تب **Deployments** داشبورد
می‌توانید پیشرفت را ببینید؛ معمولاً یکی دو دقیقه طول می‌کشد.

### اگر ENV را بعد از Deploy تنظیم کردید

متغیرهای محیطی فقط موقع build خوانده می‌شوند. اگر یادتان رفت و بعد از Deploy
اضافه‌شان کردید، باید دوباره Deploy کنید:

**Deployments** → روی آخرین Deploy → منوی سه‌نقطه → **Redeploy**

---

## مرحله ۴: راه‌اندازی اولین حساب

### ۴.۱ بازدید از سایت

پس از موفق شدن Deploy، به آدرس همیشگی برنامه بروید. این بار به‌جای ورود مستقیم،
صفحه‌ی **ورود / ساخت حساب** را می‌بینید.

### ۴.۲ ساخت حساب اول

- روی تب **ساخت حساب** کلیک کنید
- یک نام کاربری و رمز عبور وارد کنید
  - نام کاربری: ۳ تا ۳۲ کاراکتر، فقط حروف انگلیسی، عدد، نقطه، خط تیره یا زیرخط
  - رمز عبور: حداقل ۶ کاراکتر
- **کد دعوت لازم نیست** (فیلدش اصلاً نمایش داده نمی‌شود)

### ۴.۳ بازیابی داده‌ها

بعد از ورود، حساب شما داده‌های فایل `data.json` مخزن را نشان می‌دهد
(اسناد و مغازه‌ها و همکارانی که در نسخه‌ی محلی برنامه ثبت شده بودند).
این انتقال **فقط برای اولین حساب** انجام می‌شود.

اگر داده‌های سایت با چیزی که انتظار دارید فرق می‌کرد:

1. به تب **پشتیبان‌گیری و بازیابی** بروید
2. فایل JSON که در مرحله‌ی ۰ گرفتید را انتخاب کنید
3. حالت **جایگزینی کامل** را بزنید

از این لحظه به بعد داده‌ها روی Blob ذخیره می‌شوند و از هر مرورگر و دستگاهی
که با همین حساب وارد شوید در دسترس‌اند.

> 📌 **چرا ممکن است فرق کند؟**
> فایل `data.json` عکسِ داده‌های نسخه‌ی محلی برنامه است، نه سایت.
> نسخه‌ی فعلی سایت بک‌اند ندارد و داده‌هایش در localStorage مرورگر بوده،
> بنابراین اگر روی سایت چیزی وارد کرده‌اید که در نسخه‌ی محلی نبوده، فقط از
> طریق بک‌آپ مرحله‌ی ۰ برمی‌گردد.

---

## مرحله ۵: افزودن کاربر دوم

### ۵.۱ ساخت حساب با کد دعوت

از حساب اول خارج شوید (Logout)، یا از یک مرورگر/دستگاه دیگر وارد شوید.

- روی تب **ساخت حساب** کلیک کنید
- نام کاربری و رمز عبور وارد کنید
- **کد دعوت** را وارد کنید (همان `SIGNUP_CODE` که در Vercel تنظیم کردید)
- حساب جدید با یک دفتر کاملاً خالی شروع می‌شود

هر کاربر فقط داده‌های خودش را می‌بیند؛ اسناد، مغازه‌ها، همکاران و برگه‌های
صفحه‌گسترده‌ی دو حساب هیچ اشتراکی با هم ندارند.

### ۵.۲ تغییر کد دعوت

اگر بخواهید کد دعوت را تغییر دهید:

1. به **Settings** → **Environment Variables** در Vercel بروید
2. `SIGNUP_CODE` را ویرایش کنید
3. **Redeploy** کنید (یا منتظر Deploy بعدی بمانید)

---

## نکات امنیتی

✅ **حتماً `AUTH_SECRET` را محرمانه نگه دارید** — هرکس آن را داشته باشد می‌تواند نشست‌های جعلی بسازد  
✅ **کد دعوت را فقط با افرادی که می‌خواهید دسترسی داشته باشند به اشتراک بگذارید**  
✅ **رمزهای عبور قوی انتخاب کنید** (حداقل ۶ کاراکتر)  
✅ داده‌های هر کاربر کاملاً جدا از سایرین ذخیره می‌شود  

---

## عیب‌یابی

### خطای بیلد: `ERR_PNPM_OUTDATED_LOCKFILE`

پروژه از `npm` استفاده می‌کند، ولی یک فایل `pnpm-lock.yaml` قدیمی از قالب اولیه
در مخزن مانده بود. Vercel وقتی این فایل را می‌بیند سراغ pnpm می‌رود و چون
با `package.json` هماهنگ نیست، بیلد را رد می‌کند.

این فایل حذف شده و در `.gitignore` هم قرار گرفته. اگر باز هم دیدیدش:

```bash
git rm --cached pnpm-lock.yaml && rm -f pnpm-lock.yaml
```

### خطای بیلد: پکیجی پیدا نمی‌شود

اگر وابستگی جدیدی به `package.json` اضافه شد، `package-lock.json` را هم
به‌روز و کامیت کنید:

```bash
npm install
git add package.json package-lock.json
git commit -m "chore: update lockfile"
git push
```

### دکمه‌ی «ساخت حساب» کار نمی‌کند / خطای ۵۰۰ روی `/api/auth`

یعنی سرور نتوانست فهرست حساب‌ها را از Blob بخواند. روی تب **ساخت حساب** بزنید
تا متن دقیق خطا را ببینید. رایج‌ترین پیام‌ها:

| پیام خطا | معنی و راه‌حل |
|---|---|
| `This store does not exist` | توکن به یک Blob Store پاک‌شده یا اشتباه اشاره می‌کند. در **Storage** یک Blob بسازید و در **Connect Project** پروژه را وصل کنید. اگر `BLOB_READ_WRITE_TOKEN` را دستی وارد کرده‌اید، پاکش کنید تا Vercel خودش تزریقش کند. |
| `Access denied` / `valid token` | Blob Store به پروژه‌ی دیگری وصل است. آن را به همین پروژه وصل کنید. |
| `Cannot use public access on a private store` | نسخه‌ی قدیمی `@vercel/blob` نصب شده. این پکیج باید **۲.۳ یا بالاتر** باشد؛ دسترسی Private از آن نسخه به بعد پشتیبانی می‌شود. مقدار `@vercel/blob` در `package.json` را چک کنید. |
| `The requested blob does not exist` | طبیعی است و خطا حساب نمی‌شود؛ یعنی Store خالی است. اگر باعث خطای ۵۰۰ شد، نسخه‌ی کد قدیمی است — آخرین تغییرات را push کنید. |
| `Cannot find module '@vercel/blob'` | وابستگی نصب نشده. `npm install` بزنید و `package-lock.json` را کامیت کنید. |

بعد از هر تغییر در Storage یا Environment Variables، حتماً **Redeploy** کنید —
تغییرات روی Deploy فعلی اثر نمی‌گذارند.

برای دیدن جزئیات کامل: داشبورد Vercel → **Deployments** → آخرین Deploy →
تب **Logs** (بخش Functions).

### بعد از Deploy صفحه‌ی سفید یا خطای سرور می‌بینم

- در داشبورد Vercel به **Deployments** → آخرین Deploy → تب **Functions**/**Logs**
  بروید و متن خطا را ببینید
- شایع‌ترین دلیل: Blob وصل نشده یا `AUTH_SECRET` تنظیم نشده — مرحله‌های ۱ و ۲ را
  چک کنید و بعد Redeploy بزنید

### هرچه در سایت وارد می‌کنم بعد از مدتی برمی‌گردد عقب

نسخه‌ی قبلی روی Vercel اصلاً بک‌اند نداشت و داده‌ها فقط در حافظه‌ی مرورگر
(localStorage) می‌ماندند؛ به همین دلیل با پاک کردن حافظه‌ی مرورگر یا رفتن به
دستگاه دیگر، داده‌ها نبودند. در نسخه‌ی جدید همه‌چیز روی Blob ذخیره می‌شود.

اگر بعد از آپدیت هم این اتفاق افتاد، یعنی برنامه به سرور وصل نشده و روی حالت
آفلاین کار می‌کند (بالای صفحه هشدارش را نشان می‌دهد) — مرحله‌ی ۱ را چک کنید.

### داده‌های قبلی سایت را نمی‌بینم

طبیعی است. داده‌های قبلی روی سرور نبودند، در مرورگر بودند. فایل بک‌آپی که در
مرحله‌ی ۰ گرفتید را از تب «پشتیبان‌گیری و بازیابی» با حالت **جایگزینی کامل**
بازیابی کنید.

اگر بک‌آپ نگرفتید و مرورگر را هم پاک نکرده‌اید، داده‌ها هنوز همان‌جا هستند:
با همان مرورگر، کنسول را باز کنید (`F12` → تب Console) و این را اجرا کنید تا
یک فایل بک‌آپ از داده‌های قدیمی بسازد:

```js
(() => {
  const raw = localStorage.getItem("gold_accounting_state");
  if (!raw) return "چیزی پیدا نشد";
  const a = document.createElement("a");
  a.href = URL.createObjectURL(new Blob([raw], { type: "application/json" }));
  a.download = "backup-old.json";
  a.click();
})()
```

بعد فایل `backup-old.json` را از تب پشتیبان‌گیری بازیابی کنید.

### خطای "کد دعوت نادرست"

- مطمئن شوید `SIGNUP_CODE` در Environment Variables به‌درستی تنظیم شده  
- پس از تغییر، حتماً Redeploy کنید
- کد حساس به حروف بزرگ/کوچک است و فاصله‌ی اضافی هم اهمیت دارد

### تب «ساخت حساب» غیرفعال است

یعنی `SIGNUP_CODE` تنظیم نشده و حساب اول هم از قبل ساخته شده. متغیر را در
تنظیمات پروژه اضافه کنید و Redeploy بزنید.

### خطای "BLOB_READ_WRITE_TOKEN not found"

- مطمئن شوید Blob Store را در قسمت Storage به پروژه وصل کرده‌اید
- Redeploy کنید

### داده‌های قدیمی به حساب اول منتقل نشدند

- انتقال فقط برای **اولین حساب** انجام می‌شود؛ اگر قبلاً حسابی ساخته‌اید،
  حساب‌های بعدی خالی شروع می‌کنند (این رفتار عمدی است)
- مطمئن شوید فایل `data.json` در مخزن Git کامیت شده باشد — اگر کامیت نشده باشد
  به سرور نمی‌رسد
- تنظیم `includeFiles` در `vercel.json` باید دست‌نخورده بماند، وگرنه Vercel
  فایل `data.json` را همراه تابع سرور آپلود نمی‌کند
- **راه مطمئن:** بک‌آپی که در مرحله‌ی ۰ گرفتید را از تب «پشتیبان‌گیری و بازیابی»
  بازیابی کنید — این روش همیشه کار می‌کند و برگه‌های صفحه‌گسترده را هم می‌آورد

### همه‌ی حساب‌ها یک‌دفعه ناپدید شدند

به احتمال زیاد `AUTH_SECRET` عوض شده است. آن را به مقدار قبلی برگردانید و
Redeploy کنید — داده‌ها پاک نشده‌اند، فقط مسیرشان تغییر کرده بود.

### می‌خواهم از اول شروع کنم

در داشبورد Vercel، تب **Storage** → Blob Store → فایل‌های داخل پوشه‌ی `gold/`
را پاک کنید. دفعه‌ی بعد که وارد سایت شوید، دوباره صفحه‌ی ساخت حساب اول را
می‌بینید.

---

## اجرای محلی (اختیاری)

برنامه بدون هیچ تنظیمی روی سیستم خودتان هم بالا می‌آید. اگر توکن Blob وجود
نداشته باشد، داده‌ها در پوشه‌ی `.data/` کنار پروژه ذخیره می‌شوند:

```bash
npm install
npm run dev
```

سپس به `http://localhost:3000` بروید.

برای تست شدن ساخت حساب دوم به‌صورت محلی، یک فایل `.env.local` بسازید:

```
AUTH_SECRET=یک-کلید-تصادفی-برای-تست
SIGNUP_CODE=test-code
```

> پوشه‌ی `.data/` و فایل‌های `.env*` در `.gitignore` هستند و به مخزن نمی‌روند.

---

## پشتیبان‌گیری

مستقل از Blob، از تب **پشتیبان‌گیری و بازیابی** داخل برنامه می‌توانید یک فایل
JSON کامل بگیرید که شامل اسناد، تنظیمات، مغازه‌ها، همکاران و **برگه‌های
صفحه‌گسترده (اکسل داخلی)** است. همین فایل را می‌شود در هر حساب دیگری بازیابی کرد.

توصیه: قبل از هر تغییر در متغیرهای محیطی، یک بک‌آپ بگیرید.

---

## به‌روزرسانی‌های بعدی

از این به بعد، هر تغییری در کد فقط با push کردن روی سایت می‌نشیند:

```bash
git add -A
git commit -m "توضیح تغییرات"
git push
```

Vercel خودکار build و Deploy می‌کند. **حساب‌ها و داده‌ها دست‌نخورده می‌مانند**،
چون در Blob ذخیره شده‌اند نه در کد. تا وقتی `AUTH_SECRET` را عوض نکنید،
هیچ Deploy‌ای به داده‌ها دست نمی‌زند.

---

**موفق باشید! 🚀**
