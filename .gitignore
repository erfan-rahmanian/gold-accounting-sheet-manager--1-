node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# داده‌های محلی (حساب‌ها و دفاتر) — نباید کامیت شوند
.data/

# فایل‌های اصلی تصاویر (خروجی هوش مصنوعی، حدود ۸ مگابایت)
# خروجی بهینه‌شده‌شان در public/ کامیت می‌شود، پس این‌ها لازم نیست روی ورسل بروند.
# یک نسخه از این پوشه را جایی نگه دارید تا اگر خواستید با
# scripts/build-brand-assets.py دوباره بسازیدشان.
brand-src/

# فقط از npm استفاده می‌کنیم؛ lockfile سایر پکیج‌منیجرها باعث تداخل در بیلد ورسل می‌شود
pnpm-lock.yaml
yarn.lock
